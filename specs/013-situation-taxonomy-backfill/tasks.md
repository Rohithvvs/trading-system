# Tasks: Taxonomy Alignment & Historical Backfill

**Input**: Design documents from `/specs/013-situation-taxonomy-backfill/`
**Prerequisites**: [plan.md](file:///D:/Work_Space/trading-system/specs/013-situation-taxonomy-backfill/plan.md) (required), [spec.md](file:///D:/Work_Space/trading-system/specs/013-situation-taxonomy-backfill/spec.md) (required for user stories), [research.md](file:///D:/Work_Space/trading-system/specs/013-situation-taxonomy-backfill/research.md), [data-model.md](file:///D:/Work_Space/trading-system/specs/013-situation-taxonomy-backfill/data-model.md)

**Tests**: Tests are requested to prove the deterministic behavior, lack of locks, and auto-tagging precision.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

---

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [x] T001 Setup the task directory structure under specs/013-situation-taxonomy-backfill/
- [x] T002 Register the new models in backend/app/models/__init__.py

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core database model, migrations, and classifier logic that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T003 Define database models (AnalysisHistory update, BackfillProgress creation) in backend/app/models/analysis.py
- [x] T004 Create the database migration script for columns and concurrent GIN index in backend/alembic/versions/
- [x] T005 Register BackfillProgress in backend/app/db/base.py
- [x] T006 [P] Implement pure-function tagging heuristics in backend/app/services/taxonomy_classifier.py
- [x] T007 [P] Write unit tests for the tagging classifier in backend/tests/unit/test_taxonomy_classifier.py


**Checkpoint**: Foundation ready - user story implementation can now begin

---

## Phase 3: User Story 1 - Automatic Ongoing Tagging (Priority: P1) 🎯 MVP

**Goal**: Automatically classify and tag recommendations generated by the live engine at persistence time.

**Independent Test**: Run scanner CLI and check that the generated recommendation in `analysis_history` is successfully persisted with situation tags.

### Tests for User Story 1
> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T008 [US1] Write integration test verifying ongoing auto-tagging in backend/tests/integration/test_backfill_integration.py

### Implementation for User Story 1

- [x] T009 [US1] Integrate tagging logic into _persist_analysis in backend/app/agents/orchestrator_agent.py

**Checkpoint**: At this point, User Story 1 is fully functional and testable independently.

---

## Phase 4: User Story 2 - Controlled Historical Backfill (Priority: P1)

**Goal**: Retroactively tag all historical records in `analysis_history` in controlled, resumable, and non-blocking batches.

**Independent Test**: Run backfill command and interrupt it; check `backfill_progress` state. Run it again with `--resume` and verify it finishes tagging all records.

### Tests for User Story 2
> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T010 [US2] Write tests for backfill service batching, locks, and resumption in backend/tests/integration/test_backfill_integration.py

### Implementation for User Story 2

- [x] T011 [US2] Implement BackfillService keyset paging and throttling in backend/app/services/backfill_service.py
- [x] T012 [US2] Extend the CLI router in backend/app/governance/experiment_cli.py for backfill command

**Checkpoint**: At this point, User Stories 1 AND 2 work independently.

---

## Phase 5: User Story 3 - Tag Distribution & Analytical Querying (Priority: P2)

**Goal**: Expose tag distribution report via CLI and support filtering queries on situation tags.

**Independent Test**: Run `taxonomy-report` and check `stdout` table and saved file. Query recommendations by tag and verify filtered counts.

### Tests for User Story 3
> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T013 [US3] Write tests for tag distribution calculations and querying/filtering in backend/tests/integration/test_backfill_integration.py

### Implementation for User Story 3

- [x] T014 [US3] Implement distribution reporting logic in backend/app/services/backfill_service.py
- [x] T015 [US3] Extend the CLI router in backend/app/governance/experiment_cli.py for taxonomy-report command
- [x] T016 [US3] Update analytical search filters in backend/app/services/analytics_service.py

**Checkpoint**: All user stories should now be independently functional.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [x] T017 Run quickstart.md validation script in specs/013-situation-taxonomy-backfill/quickstart.md
- [x] T018 Perform cleanup of temporary test databases and logs

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately.
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories.
- **User Stories (Phase 3+)**: All depend on Foundational phase completion.
  - User Story 1 (P1) is independent of US2 and US3.
  - User Story 2 (P1) can start in parallel with US1.
  - User Story 3 (P2) depends on US1/US2 tag population to be fully meaningful, but can be developed in parallel.
- **Polish (Final Phase)**: Depends on all desired user stories being complete.

### Within Each User Story

- Tests MUST be written and FAIL before implementation.
- Models before services.
- Services before CLI endpoints.
- Core implementation before integration.

### Parallel Opportunities

- Foundational tasks `T006` and `T007` can run in parallel (tagging rules implementation & unit test writing).
- Once Phase 2 completes, User Story 1 (`T008`, `T009`) and User Story 2 (`T010`, `T011`, `T012`) can start in parallel.

---

## Parallel Example: User Story 1

```bash
# Developer A starts ongoing auto-tagging tests:
Task: "Write integration test verifying ongoing auto-tagging in backend/tests/integration/test_backfill_integration.py"

# Developer B concurrently sets up the backfill tests:
Task: "Write tests for backfill service batching and resumption in backend/tests/integration/test_backfill_integration.py"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup.
2. Complete Phase 2: Foundational (CRITICAL - blocks all stories).
3. Complete Phase 3: User Story 1.
4. **STOP and VALIDATE**: Test User Story 1 independently.

### Incremental Delivery

1. Complete Setup + Foundational → Foundation ready.
2. Add User Story 1 → Test independently → Deploy/Demo (MVP!).
3. Add User Story 2 → Test independently → Deploy/Demo (Backfill available!).
4. Add User Story 3 → Test independently → Deploy/Demo (Analytics dashboard & visibility ready!).
5. Run quickstart validation to verify completeness.
