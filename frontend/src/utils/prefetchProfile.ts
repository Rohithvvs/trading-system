/**
 * Background prefetch for User Profile — delegates to full app prefetch.
 */
export { prefetchAppData as prefetchProfileData, resetPrefetchFlag as resetProfilePrefetchFlag } from "./prefetchAppData";
